{
  "name": "lite-state-machine",
  "version": "0.0.10",
  "description": "Lite library for State Machines",
  "author": "nicosoca",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/nicosoca/lite-state-machine.git"
  },
  "scripts": {
    "format": "prettier --write \"**/*.ts\"",
    "build": "tsc",
    "start": "nodemon",
    "test": "node -r tsconfig-paths/register --require ts-node/register test/test.ts",
    "npm-publish": "yarn build && yarn version --patch && npm publish"
  },
  "devDependencies": {
    "nodemon": "^1.18.5",
    "prettier": "^1.14.3",
    "ts-node": "^7.0.1",
    "tsconfig-paths": "^3.6.0",
    "typescript": "^3.1.3"
  }
}
